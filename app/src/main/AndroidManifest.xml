<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apiallreadwritedeleteupdatemehedivi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.APIAllReadWriteDeleteUpdateMehediVi"
        android:usesCleartextTraffic="true">
        <activity android:name=".Delete_Activity"></activity>
        <activity android:name=".Update_Activity" />
        <activity android:name=".Write_Activity" />
        <activity android:name=".Read_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>